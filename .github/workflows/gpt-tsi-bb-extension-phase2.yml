name: GPT - TSI BB Extension Phase 2

on:
  workflow_dispatch:
  push:
    paths:
      - 'gpt/btc-frozen-reversal-fingerprint-validation/tsi_bb_extension_research/phase2_structural_quality_search.py'
      - '.github/workflows/gpt-tsi-bb-extension-phase2.yml'

permissions:
  actions: read
  contents: write

jobs:
  research:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install --disable-pip-version-check numpy pandas requests
      - name: Download frozen indicator artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: indicator-aug-shard-*
          path: /tmp/indicator-aug-artifacts
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: Brkzgrc/Botum_LAB
          run-id: 35292657185
      - name: Run phase 2 structural quality search
        run: |
          python gpt/btc-frozen-reversal-fingerprint-validation/tsi_bb_extension_research/phase2_structural_quality_search.py             --artifact-dir /tmp/indicator-aug-artifacts             --outdir gpt/btc-frozen-reversal-fingerprint-validation/tsi_bb_extension_research/output_phase2             --workers 10
      - name: Persist results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add gpt/btc-frozen-reversal-fingerprint-validation/tsi_bb_extension_research/output_phase2
          if git diff --cached --quiet; then
            echo "No output changes"
          else
            git commit -m "Persist TSI BB extension phase 2 [skip ci]"
            git push
          fi
